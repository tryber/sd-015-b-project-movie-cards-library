import React from "react";

class MovieCard extends React.Component {
  render() {
    const {movie} = this.props;

    return (
      <h2>Componete MovieCard</h2>
    );
  }
}

export default MovieCard;
